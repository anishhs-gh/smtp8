export default function Hero() {
  return (
    <section className="text-center space-y-3 mb-10">
      <h1 className="text-4xl font-extrabold tracking-tight text-on-surface">
        SMTP8 Workspace
      </h1>
      <p className="text-on-surface-variant max-w-lg mx-auto text-[15px]">
        Test any SMTP server in real time. Fill in the connection details, run the test, and watch
        the live protocol stream.
      </p>
    </section>
  );
}
